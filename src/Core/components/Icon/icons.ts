import { ReactComponent as Clock } from "Core/assets/icons/clock.svg";
import { ReactComponent as Loading } from "Core/assets/icons/loading.svg";
import { ReactComponent as ChevronDown } from "Core/assets/icons/chevronDown.svg";

const icons = {
  clock: Clock,
  loading: Loading,
  chevronDown: ChevronDown,
};

export default icons;
